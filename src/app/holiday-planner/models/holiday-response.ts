/**
 * Represents a public holiday from the API
 */
export interface PublicHoliday {
  /** Name of the holiday */
  name: string;
  /** Date of the holiday in ISO format (YYYY-MM-DD) */
  date: string;
}
